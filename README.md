# uniapp-starbase

根据截图生成的 UniApp 页面示例，包含：

- 产品页
- 基座控制页
- 我的页
- 我的设备页
- 自定义底部导航组件

## 使用方式

推荐使用 HBuilderX 打开 `uniapp-starbase` 目录运行到手机或浏览器。

如果使用 CLI 项目方式，可按团队现有 UniApp 工程配置接入本目录下的 `pages`、`components` 与配置文件。

## 说明

当前页面主要复刻截图中的 UI 结构与交互入口，图片使用在线占位图。后续可替换为接口返回的产品图、头像、设备图与真实蓝牙 SDK 数据。
